"use strict";
describe("myApp.viewform module", function() {
	beforeEach(module("myApp.viewform"));
	describe("viewform controller", function() { 
		it("should ....", inject(function($controller) {
			var viewformCtrl = $controller("viewformCtrl");
			expect(viewformCtrl).toBeDefined()
		}))
	})
});